{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "pnpm pre-dev && nuxt dev",
    "pre-dev": "pnpm install --shamefully-hoist",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^5.0.0-4",
    "nuxt": "3.0.0-rc.3"
  },
  "dependencies": {
    "click-outside-vue3": "^4.0.1"
  }
}
